config:
  target: 'http://127.0.0.1:3000'
  payload: 
    path: "loadtesting.csv"
    fields: 
      - "listingId"
  phases:
    - duration: 30
      arrivalRate: 1
      rampTo: 1000
scenarios:
  - flow:
    - get:
        url: "/listings/{{ listingId }}/photos"
    count: 500